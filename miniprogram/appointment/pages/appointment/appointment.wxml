<view class="container">
  <view class="header">
    <text class="title">预约挂号</text>
  </view>

  <view class="quick-entry">
    <view class="quick-item" bindtap="goToMyAppointments">
      <text class="quick-icon">📅</text>
      <text class="quick-text">我的预约</text>
    </view>
    <view class="quick-item" bindtap="goToMyDoctors">
      <text class="quick-icon">👨‍⚕️</text>
      <text class="quick-text">我的医生</text>
    </view>
  </view>

  <view class="hospital-area">
    <text class="section-title">选择院区</text>
    <view class="area-list">
      <view class="area-item {{selectedArea == item.id ? 'active' : ''}}" wx:for="{{areas}}" wx:key="id" bindtap="selectArea" data-id="{{item.id}}">
        <text class="area-name">{{item.name}}</text>
      </view>
    </view>
  </view>

  <view class="department">
    <text class="section-title">选择科室</text>
    <view class="department-container">
      <view class="department-left">
        <view class="department-main {{selectedMainDepartment == item.id ? 'active' : ''}}" wx:for="{{mainDepartments}}" wx:key="id" bindtap="selectMainDepartment" data-id="{{item.id}}">
          <text class="department-name">{{item.name}}</text>
        </view>
      </view>
      <view class="department-right">
        <view class="department-sub" wx:for="{{subDepartments}}" wx:key="id" bindtap="goToDepartmentDetail" data-id="{{item.id}}" data-name="{{item.name}}">
          <text class="department-name">{{item.name}}</text>
        </view>
      </view>
    </view>
  </view>
</view>